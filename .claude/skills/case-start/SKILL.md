---
name: case-start
description: Запуск работы над кейсом HackAlem AI. Вызывать в момент, когда капитан присылает текст кейса — скилл ведёт от разбора кейса до подачи решения: бриф, распределение лейнов, тикеты в Linear по фазам, почасовые отсечки, финальные проверки. Использовать только 23.09.2026 в соревновательное окно 13:00–18:00.
---

# Запуск работы над кейсом

Соревновательное окно **13:00–18:00**, ровно 5 часов. Всё ниже привязано к этим часам.

Общий контекст, правила хакатона и запреты — в `CLAUDE.md`. Здесь только процедура дня.

Главная мысль скилла: **тикет — это промпт для агента.** Каждый из троих работает через своего AI-агента и вставляет ему текст тикета. Другого контекста у агента нет. Слабый тикет = агент уходит не туда, расширяет объём, ломает чужие файлы. От качества тикетов зависит, соберётся проект или нет — поэтому на них не экономить.

## Как работаем с git

Ветку заводит тот, кто делает задачу — под тикет или под фазу, как ему удобнее. Закончил — мержит в main. Появился конфликт — решает его.

```bash
git checkout main && git pull
git checkout -b HACKAI-N
# работа, сколько угодно коммитов
git checkout main && git pull
git merge HACKAI-N
git push
```

Короткую задачу можно сделать и прямо в main — `git pull`, поработал, `git add` своих файлов, `git commit`, `git pull && git push`.

Два момента, которые стоит держать в голове:

- **Готовое уезжает в main сразу**, а не копится к стыку фаз. Иначе интеграция откладывается на конец, а на пятичасовом хакатоне это дорого
- **Раздел «Файлы» в тикете** нужен, чтобы двое не правили один файл. Заметил проблему в чужом лейне — скажи владельцу, а не правь сам

Перед новой задачей — `git pull`, чтобы стартовать со свежего main.

## Люди и лейны

Лейн — это **функция в проекте**, а не технология. Формы меняются от кейса к кейсу, функции — нет.

| Лейн | Что это | Форма зависит от кейса |
| --- | --- | --- |
| **Ядро** | Логика, которая делает работу | Агентный цикл, обработка, модели, данные |
| **Поверхность** | Как с решением взаимодействуют | Веб-интерфейс, CLI, HTTP API, или просто файл на входе и файл на выходе |
| **Обвязка** | Всё, что нужно для сдачи | README, Docker, смоук-тесты, демо-данные, проверка запуска, подача решения |

Обвязка существует всегда и стоит 40 баллов из 100. Ядро существует всегда. **Поверхность существует всегда, но её объём меняется в разы** — от двух часов на полноценный интерфейс до двадцати минут на CLI.

### Распределение — в фазе 0, после брифа

Жёстко закреплять людей заранее нельзя: не зная кейса, не знаешь объёмов. Есть дефолты по сильным сторонам и есть правило нарезки.

**Дефолты:** Дарья тяготеет к логике и бэкенду, Анель к интерфейсу, капитан берёт обвязку и закрывает дыры. Обвязка у капитана намеренно: работа прерываемая, а капитана дёргают орг и решения по резке. Ядро с прерываниями ломается, обвязка — нет.

**Правило нарезки.** Оценив объём поверхности по брифу:

| Если поверхность | Нарезка | Кто где |
| --- | --- | --- |
| Заметная (UI, ≥ 1.5 часа) | ядро · поверхность · обвязка | Дарья · Анель · капитан |
| Небольшая (CLI, простой API, < 1 часа) | ядро-A · ядро-B · обвязка+поверхность | Дарья · **Анель** · капитан |
| Отсутствует как отдельная работа | ядро-A · ядро-B · обвязка | Дарья · Анель · капитан |
| Ядро огромное, поверхность заметная | ядро-A · ядро-B+поверхность · обвязка | по факту брифа |

**Если ядро делится надвое — между подлейнами нужен такой же контракт, как между ядром и поверхностью.** Ядро-A и ядро-B владеют разными модулями, контракт между ними фиксируется в брифе дословно. Без этого два человека в ядре столкнутся в тех же файлах, и весь смысл разделения пропадёт.

**Кто не на ядре — тот на обвязке.** README и воспроизводимость нужны всегда и стоят 40 баллов. Их нельзя оставлять на остаток.

Распределение записывается в `BRIEF.md` и в `PROGRESS.md`, чтобы новая сессия не гадала.

## Справочник Linear

Чтобы не тратить вызовы на повторный поиск.

**Команда:** `HACKAI`, id `e5d10fef-f71e-4518-a5c6-30f1054f8783`

**Люди:**

| Кто | Linear id | email |
| --- | --- | --- |
| Azamat Yessimkhanuly | `2f38f845-b537-48e2-91f0-67a062138b23` | yessimkhanuly@gmail.com |
| Дарья | `6d137bcf-7de7-43cc-a4af-f6edd6ffd1bd` | dreaminginadream108@gmail.com |
| Анель Алибек | `8cf20273-60d4-4ef7-a153-ddb66bf9dc41` | anelalibek69@gmail.com |

**Лейблы** (уже созданы). На каждом тикете ровно: одна фаза + один лейн + `мастхэв` или `потом`, плюс спецлейблы при необходимости.

| Группа | Лейблы |
| --- | --- |
| Фаза | `фаза-0` `фаза-1` `фаза-2` `фаза-3` `фаза-4` |
| Лейн | `ядро` `поверхность` `обвязка` |
| Приоритет | `мастхэв` `потом` |
| Спец | `свободная-очередь` — никого не блокирует, берёт любой · `черновик` — написан до понимания кейса, уточнить на стыке |

Лейбл `ядро` может стоять у двоих, если ядро поделено надвое — различать по исполнителю и по разделу «Файлы». Если поверхность оказалась мелкой и ушла в обвязку, лейбл `поверхность` просто не используется.

**Статусы:**

| Статус | Значение |
| --- | --- |
| `Backlog` | Черновики будущих фаз. Не брать |
| `Todo` | Готов к взятию: текущая фаза или свободная очередь |
| `In Progress` | Взят. Один человек — один тикет в этом статусе |
| `Done` | DoD пройден, мини-тест зелёный, **запушено в main** |
| `Canceled` | Ушло в «потом». Ставить лейбл `потом`, в комментарии — когда и почему |

**Связи:** блокировки проставлять через relations Linear (blocks / blocked by) и дублировать в описании тикета в разделе «Связи».

## Шаг 1. Получить вместе с кейсом

Если чего-то нет — спросить сразу, до начала работы:

- **Текст кейса дословно**, не пересказ. Формулировки обязательных условий имеют значение
- **Методику оценки по этому кейсу**, если организатор её выдал (общие критерии уже есть в `HACKALEM-CONTEXT.md`, их запрашивать не нужно)
- **Точное время закрытия репозитория**
- Доп. инструкции с площадки

## Шаг 2. Первые 10 минут — работать не спрашивая

Две вещи одновременно. Никто никого не ждёт.

**Развернуть каркас:**

1. Скопировать шаблоны из `prep/templates/` в корень, подставив выбранный стек
2. Поставить скелет: точка входа, конфиг, `.env.example`, `requirements.txt`
3. Заполнить шапку `PROGRESS.md`: время старта, обратный отсчёт, кейс
4. **Первый коммит.** Час 1 обязан иметь подтверждение прогресса

**Разобрать кейс и написать `BRIEF.md`** в корне:

| Раздел | Что внутри |
| --- | --- |
| Проблема | Что решаем и для кого |
| Мастхэв из кейса | **Цитатами** из текста, не пересказом |
| Мастхэв из критериев | README, воспроизводимость, рабочий сквозной сценарий, проверка |
| Что на выходе | Одно проверяемое предложение: вход → результат |
| Форма поверхности | UI, CLI, API или файлы. И оценка объёма в часах |
| Нарезка лейнов | Кто на чём, по правилу из раздела «Люди и лейны» |
| Контракт | Формат данных на стыках лейнов — сигнатуры дословно |
| Стек | Выбранный, с одной строкой обоснования |
| Провайдеры | Что через OpenAI, что через NVIDIA (см. шаг 4) |
| Потом | Всё, что не влезает. Не выброшено — отложено |
| Время | Оценка по пунктам. **Сумма мастхэва ≤ 4 часов** |

Бриф пишется **за 10 минут и на одну страницу**. Если превращается в документ — съедает то время, ради которого создаётся.

Правила брифа:

- Мастхэв бывает двух видов, и второй забывают: требования кейса и требования оценки. Оба обязательны
- «Что на выходе» — проверяемое предложение, которое дословно уедет в README в «как проверить решение». Не «система для X», а «пользователь делает A и получает B»
- **Форма поверхности определяется до нарезки лейнов.** От неё зависит, кто где работает
- **Контракт фиксируется здесь и копируется в тикеты дословно.** Это единственное, что связывает лейны. Если сигнатуры в двух тикетах разойдутся — на стыке ничего не соберётся
- Поверхность делаем ровно такой, какой требует кейс. Полноценный UI только если кейс требует или без него не показать сценарий. Интерфейс — классическая яма на пять часов
- Не влезает в 4 часа — режем **сейчас**, не на четвёртом часу

## Шаг 3. Единственная точка ожидания

Капитан читает бриф и говорит «ок» или правит. Две-три минуты. Подтверждает в том числе **нарезку лейнов**.

Доменная логика стартует только после этого. Всё остальное к этому моменту уже крутится.

**Если обязательные требования не помещаются в 5 часов** — сказать вслух: режем до самого узкого сценария, который работает целиком? Наполовину сделанное не набирает баллов: оценивается только фактически работающая функциональность. Решение принимает капитан.

## Шаг 4. Провайдеры LLM

Решается один раз в брифе, не в каждом тикете.

**Правило: один провайдер в критическом пути.** Два провайдера на основном сценарии — это два способа упасть на демо. Основной — **OpenAI**: лучшая поддержка структурированного вывода и вызова инструментов, команда его знает, у каждого свой ключ.

**NVIDIA — по назначению, а не «на всякий случай».** Подключать только если кейсу нужно:

| Задача | Провайдер | Почему |
| --- | --- | --- |
| Рассуждение, агентный цикл, генерация ответа | OpenAI | Качество, structured output, tool calling |
| Эмбеддинги и реранк для RAG | NVIDIA | Есть специализированные модели, OpenAI-совместимый эндпоинт |
| Массовые дешёвые вызовы: классификация, извлечение полей | NVIDIA (открытая модель) | Кредит расходуется на порядок медленнее |
| Речь (ASR/TTS), OCR | NVIDIA | У OpenAI это дороже или отсутствует |
| Резерв, если OpenAI лёг | NVIDIA | Переключение одной переменной в `.env` |

**Одна обёртка на всех.** `app/llm.py` читает из env `LLM_PROVIDER`, `<PROVIDER>_API_KEY`, `<PROVIDER>_BASE_URL`, `<PROVIDER>_MODEL`. Оба провайдера ходят через один `openai` SDK: NVIDIA Build совместим с OpenAI API по адресу `https://integrate.api.nvidia.com/v1`. Внутри обёртки — таймауты, ретраи, JSON-режим. **Никто не зовёт SDK напрямую**, только через обёртку. Это делает провайдер настройкой, а не переписыванием.

**В тикетах** провайдер не указывается. Указывается «через `app/llm.py`». Исключения: тикет самой обёртки и тикеты, где нужна конкретная возможность (эмбеддинги → NVIDIA, с указанием модели).

**Имена моделей NVIDIA** брать с `build.nvidia.com/models` в день старта — каталог меняется. Ориентиры: `meta/llama-3.1-70b-instruct`, `nvidia/llama-3.1-nemotron-70b-instruct`, для эмбеддингов `nvidia/nv-embedqa-e5-v5`. Проверить перед использованием.

**Ключи** — только в `.env`, никогда в коде и коммитах. Перед каждым пушем: `git grep -nE "sk-[A-Za-z0-9]|nvapi-"` должен молчать.

## Шаг 5. Анатомия тикета

Каждый тикет создаётся по этому шаблону **без пропуска разделов**. Пустой раздел — написать «нет», а не удалять: агент должен видеть, что вопрос рассмотрен.

### Шаблон описания

````markdown
## Контекст
Перед началом прочитай `CLAUDE.md`, `BRIEF.md`, `PROGRESS.md`.
Начни с `git pull`. Путь в main выбираешь сам: напрямую или через ветку `HACKAI-N` (см. «Как работаем с git» в скилле).
<1–2 предложения: где этот тикет в проекте и что будет, когда он готов>

## Цель
<Одно предложение: зачем это нужно проекту>

## Что уже есть
<Файлы, функции, интерфейсы, на которые опираемся. С путями и сигнатурами.
Если ничего — «чистый лист, создаёшь с нуля»>

## Что сделать
1. <конкретный шаг>
2. <конкретный шаг>
3. ...

## Контракт
<Точные сигнатуры функций, формы данных, эндпоинты, которые этот тикет
ОБЯЗАН выставить или использовать. Дословно из BRIEF.md. Копируется в код как есть.
Если тикет не на стыке лейнов — «нет»>

## Готово когда
- [ ] <проверяемый пункт, не «работает»>
- [ ] <проверяемый пункт>
- [ ] `make check` зелёный
- [ ] запушено в main

## Мини-тест
```bash
<команда или последовательность, которая должна пройти>
```
Ожидаемый результат: <что именно увидим — строка, файл, код ответа>

## Файлы
Создать: <список>
Изменить: <список>
Не трогать: <файлы чужих лейнов>

## Не делать
- <что агент захочет сделать, но не должен>
- <соседняя проблема, которую заметит и полезет чинить>

## Связи
Блокируется: <HACKAI-N или «ничем»>
Блокирует: <HACKAI-M или «ничего»>

## Сдача
Через ветку или прямо в main — на усмотрение исполнителя. В main результат
должен оказаться сразу по готовности.
```bash
git commit -m "[HACKAI-N] <что сделано>"
git checkout main && git pull && git merge HACKAI-N && git push
```

Оценка: <N> мин
````

### Метаданные тикета в Linear

| Поле | Правило |
| --- | --- |
| Заголовок | Глагол + объект. «Реализовать обёртку над LLM», не «LLM» |
| Исполнитель | Всегда назначен. Тикет без исполнителя не существует |
| Лейблы | Ровно одна фаза + ровно один лейн + `мастхэв` или `потом`. Плюс `свободная-очередь` или `черновик` при необходимости |
| Статус | По таблице выше |
| Оценка | В минутах, в описании. 30–60 мин на тикет. Больше 60 — дробить |
| Связи | Blocks / blocked by через relations |

### Почему каждый раздел обязателен

- **Контекст** — агент стартует с нуля. Без ссылки на бриф он будет угадывать, что за проект
- **Что уже есть** — без этого агент перепишет то, что сосед уже сделал, или проигнорирует готовую функцию и напишет свою
- **Контракт** — единственный механизм интеграции между лейнами. Расхождение сигнатур обнаруживается на стыке, когда чинить дорого
- **Готово когда** — «работает» не проверяется. «Функция возвращает объект с полями X, Y на входе Z» — проверяется
- **Мини-тест** — команда, которую запускает и агент перед пушем, и сосед при проверке, и эксперт при оценке. Одна и та же
- **Файлы / Не трогать** — чтобы двое не правили один файл. Ветка от этого не спасает: конфликт всё равно всплывёт при слиянии. Три агента без этого раздела гарантированно столкнутся
- **Не делать** — агент увидит соседнюю проблему и полезет чинить. Граница объёма удерживает его в лейне
- **Связи** — чтобы освободившийся видел, что можно брать, а что заблокировано, не спрашивая
- **Сдача** — единый ритуал из четырёх команд. Без `git pull` перед `push` — отказ. Без `make check` — сломанный main для всех

### Пример готового тикета

Заголовок: **Реализовать обёртку над LLM с переключением провайдера**
Исполнитель: Дарья · Лейблы: `фаза-1` `ядро` `мастхэв` · Статус: Todo

````markdown
## Контекст
Перед началом прочитай `CLAUDE.md`, `BRIEF.md`, `PROGRESS.md`.
Начни с `git pull`. Путь в main выбираешь сам: напрямую или через ветку `HACKAI-N` (см. «Как работаем с git» в скилле).
Это первый модуль ядра. Через него пойдут все вызовы моделей в проекте;
ни один другой модуль не будет звать SDK напрямую.

## Цель
Дать проекту одну точку вызова LLM, чтобы провайдер менялся настройкой, а не кодом.

## Что уже есть
- `.env.example` с переменными `LLM_PROVIDER`, `OPENAI_*`, `NVIDIA_*`
- `app/config.py` — загрузка env через pydantic-settings (создан в фазе 0)
- Чистый лист для `app/llm.py`

## Что сделать
1. Создать `app/llm.py` с классом `LLMClient`
2. В конструкторе прочитать провайдер из `config.LLM_PROVIDER`, взять соответствующие key/base_url/model
3. Использовать `openai.OpenAI(api_key=..., base_url=...)` для обоих провайдеров
4. Реализовать `complete(messages, *, json_schema=None, temperature=0.2) -> str | dict`
5. Таймаут 30 с, ретраи 2 раза с экспоненциальной паузой на сетевых и 5xx ошибках
6. При `json_schema` — включать JSON-режим и валидировать ответ через pydantic; при невалидном JSON — один повторный запрос с просьбой исправить

## Контракт
```python
class LLMClient:
    def __init__(self) -> None: ...
    def complete(
        self,
        messages: list[dict[str, str]],
        *,
        json_schema: type[pydantic.BaseModel] | None = None,
        temperature: float = 0.2,
    ) -> str | pydantic.BaseModel: ...

def get_client() -> LLMClient: ...  # синглтон
```

## Готово когда
- [ ] `get_client().complete([{"role":"user","content":"Скажи ok"}])` возвращает непустую строку при `LLM_PROVIDER=openai`
- [ ] То же при `LLM_PROVIDER=nvidia` — переключение только через `.env`
- [ ] С `json_schema=` возвращает валидный экземпляр модели
- [ ] Отсутствие ключа даёт понятную ошибку `LLMConfigError`, а не трейс SDK
- [ ] `make check` зелёный
- [ ] Запушено в main

## Мини-тест
```bash
python -c "from app.llm import get_client; print(get_client().complete([{'role':'user','content':'Ответь одним словом: ok'}]))"
```
Ожидаемый результат: в stdout строка, содержащая `ok`, без трейсбека.

## Файлы
Создать: `app/llm.py`, `tests/test_llm.py`
Изменить: `requirements.txt` (добавить `openai`, `pydantic`)
Не трогать: файлы поверхности, `app/main.py`, `Dockerfile`, `README.md`

## Не делать
- Не писать доменную логику — только обёртка
- Не добавлять кэширование, стриминг, async — не нужно в этой фазе
- Не хардкодить имена моделей — только из config
- Не ловить все исключения подряд; только сетевые и 5xx

## Связи
Блокируется: ничем
Блокирует: HACKAI-<ядро: основная функция>, HACKAI-<поверхность: подключение реального ответа>

## Сдача
```bash
git add app/llm.py tests/test_llm.py requirements.txt
git commit -m "[HACKAI-N] Обёртка над LLM с переключением провайдера"
git checkout main && git pull && git merge HACKAI-N && git push
```

Оценка: 40 мин
````

## Шаг 6. Стартовый набор тикетов

Каркас, не зависящий от кейса. В день старта — заполнить `<…>` из брифа, скорректировать под кейс и под выбранную нарезку лейнов, создать. Фазы 0–1 детально, фазы 2–4 с лейблом `черновик` и статусом Backlog.

Ниже — заголовки. Полное описание каждого тикета пишется по шаблону из шага 5.

Если поверхность ушла в обвязку, её тикеты переписываются под выбранную форму (CLI, API, файлы) и достаются тому, кто на обвязке; освободившийся человек берёт вторую половину ядра.

### Фаза 0 · 13:00–13:20

| Лейн | Тикет | Мин |
| --- | --- | --- |
| обвязка | Развернуть каркас из `prep/`, конфиг, первый коммит | 15 |
| все | Прочитать `BRIEF.md`, подтвердить контракт и нарезку лейнов | 5 |

### Фаза 1 · 13:20–14:30 — сквозной скелет

Цель фазы: **вход → заглушка → выход работает целиком.** Внутри пусто, снаружи — весь путь.

| Лейн | Тикет | Мин |
| --- | --- | --- |
| ядро | Реализовать обёртку над LLM с переключением провайдера | 40 |
| ядро | Создать основную функцию `<из контракта>` с заглушкой, возвращающей валидную форму | 20 |
| поверхность | Поднять `<форма поверхности из брифа>`: принимает вход, показывает результат | 40 |
| поверхность | Подключить поверхность к основной функции ядра через контракт | 20 |
| обвязка | Docker собирается, `make run` поднимает, `make check` проходит на заглушке | 25 |
| обвязка | Подготовить демо-данные: `<3–5 примеров входа из кейса>` в `data/` | 20 |
| обвязка | README: шапка, описание, «что на выходе» из брифа | 15 |

Блокировки: обёртка → основная функция → подключение поверхности. Каркас поверхности и вся обвязка — параллельно, ничем не блокируются.

### Фаза 2 · 14:30–16:00 — настоящая логика

| Лейн | Тикет | Мин |
| --- | --- | --- |
| ядро | Заменить заглушку: `<шаг 1 логики из мастхэва кейса>` | 40 |
| ядро | `<шаг 2 логики>` | 40 |
| поверхность | Реальный ввод: `<по форме поверхности>`, валидация | 40 |
| поверхность | Отображение реального результата в удобном для жюри виде | 30 |
| обвязка | Смоук-тест на реальный сценарий с демо-данными | 30 |
| обвязка | **Подать решение черновиком через «Сдать решение»** — до 15:00 | 10 |
| обвязка | README: «что реализовано», «как работает», «как проверить» | 30 |

### Фаза 3 · 16:00–17:00 — надёжность

| Лейн | Тикет | Мин |
| --- | --- | --- |
| ядро | Обработка некорректного входа: пусто, мусор, слишком длинно | 30 |
| ядро | `<edge case из кейса>` | 30 |
| поверхность | Состояния загрузки и ошибок, понятные сообщения | 30 |
| поверхность | Полировка демо-пути: то, что жюри увидит первым | 30 |
| обвязка | Чистая сборка без кеша, прогон по README как чужой человек | 30 |
| обвязка | README: «ограничения» из списка «потом», «данные и интеграции» | 20 |

### Фаза 4 · 17:00–18:00 — заморозка

| Лейн | Тикет | Мин |
| --- | --- | --- |
| все | **Заморозка функциональности.** Только README, проверка, слияние | — |
| обвязка | Финальные проверки из шага 12 | 20 |
| обвязка | README: «ранее созданные материалы», финальная вычитка против репозитория | 20 |
| обвязка | Обновить подачу до финальной версии | 5 |
| остальные | Прогон README на своей машине, правки | 30 |

### Свободная очередь · лейбл `свободная-очередь`, статус Todo с самого начала

Берёт любой освободившийся. Держать заполненной весь день.

- Ещё демо-примеры в `data/`
- Тест на `<функция>` с граничным входом
- Раздел README `<название>`
- Актуализировать `.env.example` под реально используемые переменные
- Деплой на бесплатный хостинг, если основное готово
- Схема архитектуры текстом для README

## Шаг 7. Фазы и что они значат

| Фаза | Время | Цель |
| --- | --- | --- |
| 0 | 13:00–13:20 | Бриф, нарезка лейнов, контракт, скелет, первый коммит |
| 1 | 13:20–14:30 | **Сквозной скелет**: вход → заглушка → выход работает целиком |
| 2 | 14:30–16:00 | Настоящая логика вместо заглушек, обязательные требования кейса |
| 3 | 16:00–17:00 | Доработка, обработка ошибок, надёжность |
| 4 | 17:00–18:00 | README, чистая сборка, проверка, подача |

**Фаза 0 фиксирует контракт и нарезку.** После этого каждый работает, не заглядывая к соседу.

**Фаза 1 — сквозной скелет, а не первая готовая функция.** Сначала работает путь целиком с заглушками, потом заглушки заменяются. Так риск интеграции уезжает в начало, и на любом часу есть что показать. Обратный порядок: на четвёртом часу всё написано, но вместе не собирается.

## Шаг 8. Против простоя — три правила

**1. Слияние непрерывное.** Лейны разделены по файлам, каждый готовый тикет уходит в main сразу, независимо от того, шёл он напрямую или через ветку. Состояния «закончил и жду слияния» не существует. Стык фаз — это проверка и перепланирование, а не момент слияния.

**2. Закончил раньше — три шага строго по порядку.**

Фаза не является стеной для личной работы. Это точка сверки для команды. Закончил своё — работаешь дальше, а не ждёшь.

1. **Свободная очередь.** Берётся первой: там мастхэв по критериям — демо-данные, смоук-тесты, README, проверка сборки. Писать новую функциональность, когда README не начат, — плохой размен
2. **Следующий тикет в своём лейне**, даже если он формально из следующей фазы. Но только **не заблокированный чужой незаконченной работой**. Для этого у тикетов проставлены блокировки — чтобы не гадать, а видеть

   Пример: поверхность закончила фазу 1. В фазе 2 есть «подключить реальные данные» — зависит от ядра, брать нельзя. И есть «добавить обработку неверного ввода» — только свои файлы, берётся и делается
3. **Помочь отстающему**, но только отделяемым куском, **в своих файлах или в новом файле**. Чужие файлы не трогать — ветка от конфликта не спасёт

**Не убегать больше чем на одну фазу вперёд.** Работа на две фазы вперёд строится на предположениях, которые изменятся, и её придётся переделывать. В такой момент — свободная очередь или помощь другому.

Сверка происходит **по часам, а не по готовности**. Кто-то ушёл вперёд — на стыке это видно, и расписание при необходимости сжимается.

**3. Отстаёшь больше 15 минут — говоришь сразу**, не дожидаясь стыка. Тогда либо дробим тикет и часть уходит свободному, либо переносим остаток. Молчаливое отставание обнаруживается на стыке, когда перестраиваться уже дорого.

## Шаг 9. Стык фаз — 5 минут, порядок фиксированный

1. Всё запушено, **main зелёный**: сборка и смоук проходят
2. Сверка с брифом: какие мастхэвы закрыты, какие остались
3. **Время: успеваем или режем.** Что уходит в «потом» — решает капитан
4. Тикеты следующей фазы: снять `черновик`, уточнить по факту, перевести из Backlog в Todo, лишнее — в Canceled с лейблом `потом`
5. Состояние в `PROGRESS.md`, коммит

Пункты 1–3 Claude готовит заранее, к моменту сбора сводка уже на руках.

**Главное на стыке — не «что добавить», а «что режем».** Если сверка сводится к добавлению задач, объём растёт монотонно и к пятому часу не доделано ничего. Добавлять стоит только пропущенное **обязательное требование кейса**. Всё «хорошо бы» — в «потом», без обсуждения.

Перенос в «потом» это не потеря: он же станет разделом ограничений в README и ответом на критерий «потенциал развития» на Demo Day.

## Шаг 10. Жёсткие отсечки — по часам, независимо от готовности

| Время | Что происходит |
| --- | --- |
| **к 15:00** | Решение подано черновиком через «Сдать решение». Дальше обновляется |
| **17:00** | **Заморозка функциональности.** Что не работает — в «потом». Дальше только README и проверка |
| **17:30** | Сборка с нуля без кеша, прогон сценария |
| **17:45** | Финальные коммиты |

Подача на втором часу убирает целый класс катастроф: «сделали, но забыли нажать кнопку». Решение обновляется до дедлайна, так что подать рано ничего не стоит.

Без отсечки на 17:00 команда кодит до последней минуты, а README и проверка запуска не случаются. Это 40 баллов, потерянных из-за отсутствия будильника.

## Шаг 11. Что Claude делает непрерывно весь день

- Ведёт `PROGRESS.md` — состояние, а не отчёт
- Двигает тикеты в Linear, держит свободную очередь заполненной
- Готовит сводки к стыкам фаз заранее
- Напоминает про отсечки
- Следит, что **у всех троих есть собственные коммиты** — без этого участие не засчитывается персонально. Проверка: `git shortlog -sn` должен показывать три имени
- Напоминает делать `git pull` перед новой задачей

## Шаг 12. Перед закрытием репозитория — механические проверки

- [ ] `git grep -nE "sk-[A-Za-z0-9]|nvapi-"` молчит — ключей в репозитории нет
- [ ] `git shortlog -sn` показывает три имени — у каждого есть коммиты
- [ ] README не описывает ничего, чего нет в коде
- [ ] `make verify` проходит — сборка с нуля и сценарий
- [ ] Раздел «Ранее созданные материалы» в README заполнен — заготовки из `prep/` раскрыты
- [ ] Решение подано через «Сдать решение» и обновлено до финальной версии
