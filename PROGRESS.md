# Состояние работы

Снимок состояния для продолжения работы, не отчёт о достижениях.
Формат записи: что работает, что сломано, какое решение принято и почему, что дальше.

Основное подтверждение почасового прогресса для организатора — **коммиты**, а не этот файл.
Здесь фиксируется только то, что правда. Формальные записи — основание для дисквалификации (п. 6.6).

---

## Текущее состояние

<!-- Новая сессия читает этот блок первым. Держать в актуальном состоянии
весь день — по нему определяется, что вообще происходит. -->

**Статус:** соревновательная часть **идёт**. **Фактическое окно 13:30–18:30** (объявлено на площадке). Отсечки: подача черновика **15:30** · заморозка функциональности **17:30** · чистая сборка 18:00 · финальные коммиты 18:15 · закрытие **18:30**.

**Текущая фаза:** 1 — сквозной скелет (до 15:00; фазы сдвинуты на +30 мин: 2 до 16:30, 3 до 17:30, 4 до 18:30). Фаза 0 закрыта коммитом `e7d94f0` (первый коммит соревновательной части, время в git).

**Кейс:** трек 07, **«AI Sana Challenge Hub: от бизнес-задачи к решению»** (владелец МНВО — AI Sana). Текст: `HackAlem_AI_кейс_по_геймификации_практических_заданий.pdf`, разбор — `BRIEF.md`.

**Стек:** Python 3.12 · FastAPI + Jinja2 (серверный HTML) · JSON-хранилище `data/store.json` из сида `data/seed.json` · OpenAI SDK через `app/llm.py`, без ключа — локальная заглушка.

**Потоки (лейблы Linear):** `ядро` — Дарья (`app/llm.py`, `app/ai.py`, `app/rating.py`, `app/prompts.py`) · `поверхность` — Анель (`app/main.py`, `app/templates/**`, `app/static/**`) · `обвязка` — Азамат (`app/models.py`, `app/store.py`, `data/**`, Docker, README, тесты сквозного сценария, подача).

**Что работает (main, 14:11, 45 passed / 1 skipped):**

| Слой | Готово |
| --- | --- |
| Ядро | Обёртка LLM с переключением провайдера и режимом без ключа (HAC-6) · рейтинг по 7 показателям с расшифровкой и подсказками (HAC-7) · уточняющие вопросы по пробелам рейтинга, LLM + заглушка (HAC-8) |
| Обвязка | Модели, JSON-хранилище, сид 5×4 сущностей, Docker и запуск без Docker, тесты хранилища, демо-сценарий с точными баллами, запасные примеры, схема архитектуры, инструкции для Codex (HAC-29) |
| README | Описание, сценарий, **формула рейтинга**, правила каталога, **AI-функции с промптом и обработкой сбоев**, установка, проверка, данные, ранее созданные материалы |
| Поверхность | **Ничего.** Главная и `/health` из фазы 0 |

**Проверки воспроизводимости и безопасности (13:56–14:10):** сборка без кеша exit 0 · тесты в контейнере зелёные · запуск без Docker в чистом venv на Python 3.14 зелёный · секретов в репозитории нет · `.env` в образ не попадает · образ почищен от рабочей документации и PDF.

**Демо-числа по настоящей формуле:** слабый черновик + ответы на два вопроса = **30 «Черновик»**; после дозаполнения = **100 «Приоритетная»**. Тексты в `data/demo-scenario.md`.

**Главный риск — поверхность.** HAC-10 (конструктор) дописан у Анель локально на ветке `work`, но **не влит в main**: ветка отведена от фазы 0, инструкция по слиянию дана комментарием в тикете. Следствие: в соревновательном окне у Анель **ноль коммитов**, а весь демо-путь (конструктор, редактор, каталог, страница задачи) отсутствует. Пока это не влито, HAC-16 (сквозной тест) делать не на чем.

**В работе:** HAC-9 сборка карточки (Дарья, последний тикет ядра фазы 2) · HAC-10 → HAC-11 (Анель) · README и проверки (капитан).

**Следующий шаг:** Анель — влить HAC-10 в main, затем HAC-11 (прочитать секцию «Уточнения от капитана» про подтверждение балла). Дарья — HAC-9, затем HAC-19 (устойчивость ИИ) в своих файлах, чтобы не пересекаться с поверхностью. Капитан — подача HAC-17 к **15:30**, стык фаз в 15:00.

**План Б, если к 15:30 HAC-11 не готов:** каталог HAC-12 забирает капитан — он трогает в основном новый шаблон `catalog.html` и один маршрут, пересечение с редактором минимальное.

---

## Почасовая сетка

Заполняется по ходу соревновательной части. Время — Астана.

| Час | Время | Состояние на конец часа | Коммит |
| --- | --- | --- | --- |
| 0 | 13:00–13:27 | Кейс разобран, бриф и контракт в `BRIEF.md`, каркас FastAPI + модели + хранилище + заглушки ядра, 4 теста зелёных, тикеты в Linear | `e7d94f0` |
| 1 | 13:27–14:11 | Ядро: обёртка LLM, рейтинг, уточняющие вопросы (HAC-6/7/8). Обвязка: Docker, сид, демо-сценарий с точными баллами, тесты хранилища, проверки воспроизводимости, инструкции для Codex. README: формула рейтинга и AI-функции. Поверхность не влита | `134aa64`, `d78cf76`, `467344b`, `f25ba8b`, `ef3f40d` |
| 2 | | | |
| 3 | | | |
| 4 | | | |
| 5 | | | |

---

## Принятые решения

Что решили и почему. Нужно, чтобы не переобсуждать одно и то же и чтобы README потом собирался из готового.

| Решение | Причина | Когда |
| --- | --- | --- |
| Трек 07 «Образование» | Выбор команды, не окончательный | до старта |
| Docker как основной путь запуска + запасной путь без него | Критерий воспроизводимости, 20 баллов. Запасной путь — страховка от окружения эксперта | до старта |
| Ветку заводит исполнитель, как ему удобно; готовое сразу в main | Без ревью и PR — на это нет времени. Интеграция не откладывается на конец | до старта |
| Потоки работы и лейны нарезаются в фазе 0, а не заранее | Слои решения видно только по кейсу. Жёсткое закрепление людей за ролями привело бы к простою и к работе не над тем | до старта |
| Один провайдер в критическом пути — OpenAI | Два провайдера на основном сценарии = два способа упасть. NVIDIA по назначению: эмбеддинги, массовые вызовы, речь, резерв | до старта |
| Рейтинг: `score/level` карточки — только подтверждённый балл; редактор показывает предварительный | Кейс: «баллы только за заполненные и подтверждённые поля». Уточнение капитана по ревью брифа, 13:50 | 13:50 |
| NVIDIA-резерв не входит в приёмку | Наше решение, не требование кейса; при нехватке времени не проверяем | 13:50 |
| FastAPI + Jinja2, серверный HTML, без Node | Один процесс, один порт, одна команда запуска; три лейна делятся по файлам без конфликтов | 13:25 |
| JSON-хранилище вместо БД | Сид-данные кейса (5×4 сущностей) — это буквально начальный файл; SQL за 5 часов не окупается | 13:25 |
| Рейтинг детерминированный, без LLM | Критерий «прозрачен, пересчитывается» на 25 баллов — правила должны быть объяснимы в README и покрыты тестами | 13:25 |
| ИИ: две функции (вопросы, сборка карточки) + локальная заглушка без ключа | Кейс требует минимум одну и разрешает заглушку; эксперт без ключа всё равно проходит сценарий | 13:25 |
| `.env` необязателен для `docker compose up` | Воспроизводимость: без ключа — режим заглушки, а не падение | 13:35 |

---

## Известные проблемы и ограничения

Сюда пишем по ходу всё, что не успели, отложили или знаем как слабое место.
На пятом часу этот список дословно уезжает в README в раздел ограничений — он оценивается.

| Что | Статус |
| --- | --- |
| | |
