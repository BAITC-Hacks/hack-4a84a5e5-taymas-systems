# Состояние работы

Снимок состояния для продолжения работы, не отчёт о достижениях.
Формат записи: что работает, что сломано, какое решение принято и почему, что дальше.

Основное подтверждение почасового прогресса для организатора — **коммиты**, а не этот файл.
Здесь фиксируется только то, что правда. Формальные записи — основание для дисквалификации (п. 6.6).

---

## Текущее состояние

<!-- Новая сессия читает этот блок первым. Держать в актуальном состоянии
весь день — по нему определяется, что вообще происходит. -->

**Статус:** соревновательная часть **идёт**. **Фактическое окно 13:30–18:30** (объявлено на площадке). Отсечки: подача черновика **15:30** · заморозка функциональности **17:30** · чистая сборка 18:00 · финальные коммиты 18:15 · закрытие **18:30**.

**Текущая фаза:** **3 — надёжность и полировка**, началась в 14:50 с опережением плана на полтора часа (по сетке фаза 3 — 16:30–17:30). Фаза 2 закрыта в 14:55 зелёным гейтом; стык 2→3 проведён 14:45–14:50 по полному тексту ТЗ. Заморозка функциональности **17:30** остаётся жёсткой. Фаза 0 закрыта `e7d94f0`, фаза 1 — `600b306`, фаза 2 — `4b0e0a2`.

**Кейс:** трек 07, **«AI Sana Challenge Hub: от бизнес-задачи к решению»** (владелец МНВО — AI Sana). Текст: `HackAlem_AI_кейс_по_геймификации_практических_заданий.pdf`, разбор — `BRIEF.md`.

**Стек:** Python 3.12 · FastAPI + Jinja2 (серверный HTML) · JSON-хранилище `data/store.json` из сида `data/seed.json` · OpenAI SDK через `app/llm.py`, без ключа — локальная заглушка.

**Надстройки после закрытия ТЗ (с 16:05).** Обязательная часть закрыта в 16:05 (135 тестов, чистая сборка). Дальше команда добавляет геймификацию поверх готового демо-пути, потоками в своих файлах:

| Кто | Что | Статус |
| --- | --- | --- |
| Анель | HAC-55 редизайн: три цветотипа, шапка, главная, конструктор, редактор с кольцом рейтинга и наклейками-достижениями, сворачиваемые пояснения, окно «Как считается рейтинг», тур «Как это работает» | вливается по частям, последнее `8be0215` (16:51) |
| Дарья + Анель | HAC-26 + HAC-52 рекомендации задач командам: `app/recommend.py`, блок «Подходит команде» в каталоге | закрыто 16:19 |
| Дарья | Подсказка «Не хватает: …» у каждой неполной задачи в каталоге по `next_best_action` | влито `58ae40e` (16:44) прямо в main, без номера тикета; описано в README «Правила каталога» |
| Claude капитана | **HAC-56 испытание задачи** (закрыт 16:43): виртуальная команда из сида планирует первую неделю только по карточке; вердикт и блокеры детерминированы (контекст, данные, результат, пользователи), план и причины пишет модель, без ключа — заглушка с цитатами, числа не из карточки отбрасывают ответ модели; `GET/POST /business/cards/{id}/trial`, знак «прошла испытание» в каталоге и на странице задачи; проверено на реальном ключе, скриншоты 10–12 в README. Плюс «Подходящие команды» на странице задачи и подсказка команды на странице испытания (обратная сторона HAC-26) | закрыто: `b22d0ca` `2180ef0` `97dbc55` `9387008`; README, BRIEF, чеклист приёмки, демо-сценарий обновлены |
| Вторая сессия капитана (Codex, HAC-57) | **Живой каталог**: `/catalog.json`, `/feed.json`, анимированная перестановка без F5, лента на главной, прогноз позиции и баннер повышения уровня в редакторе | влито `d43dc47` (16:41); раздел README ещё в рабочем дереве. **Сверх тикета** пишет `app/assistants.py`, `app/web_assistants.py`, шаблон, JS и `tests/test_assistants.py` (поиск для студентов, коуч, план миссии через `app/llm.py`) — не влито, в `main.py` не подключено; судьба решается на заморозке 17:30 |
| Третья сессия капитана (Claude, HAC-58) | **Черновик из фото**: на «Разместить задачу» снимок, скриншот, Ctrl+V или перетаскивание → `POST /business/photo` → модель по строгой схеме описывает **только видимое**, текст ложится в поле черновика, человек правит и отправляет как обычно. Ничего не сохраняется. Без ключа честный отказ, не заглушка; NVIDIA-резерв отклоняется до вызова (текстовая модель). Рамка — раздел 5 ТЗ: ИИ не решает, что делать, и не добавляет фактов. Claude возражал (риск по «выдуманным данным», домен кейса, время), капитан решил делать в безопасной форме. Проверено живым вызовом gpt-4.1-mini на скриншоте главной: текст экрана пересказан дословно, «не определить» перечислено. Файлы: `app/vision.py`, `app/web_vision.py`, `app/static/photo.js|css`, `app/templates/_photo_attach.html`, `tests/test_vision.py`; по строке в `main.py` и `business_new.html` | влито `548c1e3` (16:58), 13 тестов; README `cf21bc2`; затем фото переписано другой сессией под тем же тикетом: `5a3e629` |
| Третья сессия капитана (Claude, HAC-59) | **Сравнение откликов без ранжирования**, шаг 7 кейса: `GET /tasks/{id}/compare`, отклики колонками; факты детерминированно (срок, прототип, шаги плана, слова, технологии из профиля команды в отклике), разбор моделью только по тексту (сильное, вопросы команде, риски); строки с формулировками выбора и с чужими числами отбрасываются, выдуманные id тоже. Без ключа только факты, ничего не сохраняется. Проверено живым вызовом на карточке сида с двумя откликами. Файлы: `app/compare.py`, `app/web_compare.py`, `app/templates/compare.html`, `tests/test_compare.py`; по строке в `main.py` и `task.html` | код влит `b04c21f` (17:22), 7 тестов; README: раздел под «AI-функции», сценарий 7, ограничение |

**Для новой сессии важно:** вторая сессия капитана работает **в том же рабочем каталоге**, в дереве могут лежать чужие незакоммиченные файлы. Коммитить только свои файлы по именам, тянуть `git pull --rebase --autostash`, никогда `git add -A`. Заморозка функциональности **17:30** остаётся жёсткой: что не влито к 17:30, откатывается; 18:00 чистая сборка; 18:30 закрытие.

**Инцидент 16:58, урок для всех сессий.** `git pull --rebase --autostash` из третьей сессии при чужих незакоммиченных правках в дереве: автостэш вернулся с конфликтом в `app/templates/index.html` (незакоммиченная вставка второй сессии против влитых `dfd120e` и `d559c18`). Маркеров в файле не осталось, чужие файлы возвращены в незастейдженное состояние; автостэш оставлен в списке (`stash@{0}`), его содержимое уже в дереве, снять его можно вручную командой `git stash drop`. Правило: перед `pull` смотреть `git status`; если чужие правки лежат в файлах, которые менялись в origin, сначала дать их владельцу закоммитить.

**Инцидент 17:22, README.** Коммит `b04c21f` (HAC-59) застейджил через индекс устаревшую копию README из scratch: скрипт вставок упал на исчезнувшем якоре (раздел фото к тому моменту переписала другая сессия), а оболочка не остановилась. Из README на один коммит выпали разделы HAC-57 и новый раздел фото. Рабочая копия была верной; следующий коммит возвращает её целиком и добавляет разделы HAC-59. Урок: после скрипта проверять код возврата, а перед коммитом README сверять `git diff --cached` на удаления.

**Потоки (лейблы Linear):** `ядро` — Дарья (`app/llm.py`, `app/ai.py`, `app/rating.py`, `app/prompts.py`) · `поверхность` — Анель (`app/main.py`, `app/templates/new.html|questions.html|card_edit.html`, `app/static/style.css`, `tests/test_web.py`) · `обвязка` — Азамат (`app/models.py`, `app/store.py`, `data/**`, Docker, README, тесты сквозного сценария, подача) **+ забранная половина поверхности**: `app/web_catalog.py`, `app/templates/catalog.html|task.html`, `app/static/catalog.css`, `tests/test_web_catalog.py`.

**Перенарезка 14:25.** Капитан забрал у поверхности HAC-12 (каталог) и HAC-13 (страница задачи). Причина — арифметика: 170 минут работы веб-слоя на одного человека при трёх часах до заморозки в 17:30. У Анель осталось влить HAC-10 и сделать HAC-11. Чтобы два человека не делили один файл, маршруты каталога и задачи вынесены в отдельный роутер `app/web_catalog.py`, подключённый в `app/main.py` одной строкой `app.include_router(catalog_router)`; стили и тесты тоже в своих файлах. Контракт маршрутов из `BRIEF.md` не менялся.

**Что работает (main, 16:05, 135 passed, 0 skipped):**

| Слой | Готово |
| --- | --- |
| Ядро | **Закрыто целиком.** Обёртка LLM с переключением провайдера и режимом без ключа (HAC-6) · рейтинг по 7 показателям с расшифровкой и подсказками (HAC-7) · уточняющие вопросы по пробелам рейтинга, LLM + заглушка (HAC-8) · сборка карточки из ответов через LLM с защитой от выдуманных фактов, заглушка целиком при сбое (HAC-9) |
| Обвязка | Модели, JSON-хранилище, сид 5×4 сущностей, Docker и запуск без Docker, тесты хранилища, демо-сценарий с точными баллами, запасные примеры, схема архитектуры, инструкции для Codex (HAC-29) |
| README | Описание, сценарий, **формула рейтинга**, правила каталога, **AI-функции с промптом и обработкой сбоев**, установка, проверка, данные, ранее созданные материалы |
| Поверхность (забрано капитаном) | Каталог: сортировка по подтверждённому баллу, фильтры по теме и уровню, пометка «требует уточнения» у черновиков, выделение приоритетных, мусор в фильтрах не роняет страницу (HAC-12) · Страница задачи: поля карточки, расшифровка рейтинга по 7 показателям, недостающие сведения, отклики команд без ограничения числа, форма отклика с валидацией, **ручное** решение бизнеса по каждому отклику отдельно (HAC-13) |
| Поверхность (Анель) | Конструктор: черновик → 6 уточняющих вопросов → карточка (HAC-10, коммит `600b306`) · Редактор с панелью рейтинга, пересчётом и публикацией (HAC-11, коммит `eef81fa`) |

**Проверки воспроизводимости и безопасности (13:56–14:10):** сборка без кеша exit 0 · тесты в контейнере зелёные · запуск без Docker в чистом venv на Python 3.14 зелёный · секретов в репозитории нет · `.env` в образ не попадает · образ почищен от рабочей документации и PDF.

**Демо-числа по настоящей формуле:** слабый черновик + ответы на два вопроса = **30 «Черновик»**; после дозаполнения = **100 «Приоритетная»**. Тексты в `data/demo-scenario.md`.

**Главный риск — снят.** Ниже прежняя формулировка оставлена как след: она описывала состояние до 14:55.

**Главный риск (снято 14:55) — вход в сценарий.** Хвост демо-пути (каталог → отклик → решение бизнеса) закрыт и покрыт тестами. Не закрыто начало: HAC-10 (конструктор) по-прежнему лежит у Анель на локальной ветке `work` и **не влит в main** с 13:55, HAC-11 (редактор с публикацией) не начат. Следствия: у Анель **ноль коммитов в соревновательном окне** — это её личный риск незачёта участия, и закрыть его за неё нельзя, ветка только на её машине; без публикации карточку нельзя довести до каталога живьём, поэтому HAC-16 (сквозной смоук) пока делать не на чем.

**В работе:** HAC-10 → HAC-11 (Анель) · подача HAC-17 и README (капитан). Ядро (Дарья) свободно, следующий HAC-19.

**Следующий шаг:** Анель — влить HAC-10, затем HAC-11 (прочитать в нём «Уточнения от капитана» про подтверждение балла; редирект после публикации вести на `/tasks/{id}`, страница уже есть). Капитан — HAC-17 к **15:30**, затем HAC-16. Дарья — HAC-19 в своих файлах.

**Стык фаз 2→3, 14:45–14:50 — по полному тексту ТЗ.** Капитан выдал оригинал кейса (6 страниц), `BRIEF.md` переписан по нему дословно, конспект фазы 0 заменён. Сверка построчная: **все обязательные модули раздела 3 и переходы 1–7 раздела 2 закрыты и проверены**; шаг 8 «Результат» сознательно не делается по разделу 7 ТЗ. Фаза 2 закрыта и против оригинала.

Что изменил полный текст — три вещи, которых в конспекте не было:

| Найдено в ТЗ | Что меняет | Тикет |
| --- | --- | --- |
| Рубрика кейса: **«Качество карточки» 15** — «вопросы уместны; карточка полная и редактируемая» | Английские подписи конструктора и редактора бьют прямо сюда | **HAC-30** новый, Анель, первым делом |
| Рубрика кейса: **«Техническое качество» 10** — «есть запуск, **валидация**, структура и инструкция» | Валидация оценивается отдельно, HAC-20 из `черновик` в мастхэв | HAC-20 Анель · HAC-31 капитан (отклик) |
| Раздел 10: «README с … **тестовыми сценариями**» | Раздел нужен под этим именем, у нас «Как проверить решение» | HAC-18 капитан |

Рекомендации команд упомянуты в ТЗ четыре раза, но всегда «может / разрешено / или» и ни в одном критерии рубрики — HAC-26 остаётся `потом`, с условием «≥ 40 баллов» из раздела 4.

**Тикеты фазы 3 — полная нарезка 14:58** (капитан попросил мелко и целиком: фаза 2 закрылась на полтора часа раньше, у троих по 2,5 часа до заморозки). Порядок внутри строки — порядок выполнения.

| Кто | Тикеты по порядку | Мин |
| --- | --- | --- |
| Анель | HAC-20 валидация форм → HAC-34 подсказки в полях редактора → HAC-21 наглядность демо → HAC-35 `/health` для эксперта → HAC-36 индикатор резервного режима (после HAC-19) | 40+15+30+10+10 |
| Дарья | HAC-19 устойчивость ИИ (в работе) → **HAC-32 прогон с реальным ключом** (единственный skipped-тест, за день не запускался) → HAC-33 уместность заглушки без ключа → HAC-26 рекомендации только если время до 17:00 | 30+20+25 |
| Капитан | HAC-43 гигиена репо → HAC-37 тест сида → HAC-38 тест экранирования → HAC-39 позиция «N из M» и число откликов → HAC-23 скриншоты (после HAC-21) → HAC-40 прогон demo-scenario дословно (после HAC-33, HAC-21) → HAC-41 README ограничения и «что дальше» (17:15) → HAC-22 прогон 2 (18:00) → HAC-42 README «Демонстрация» → HAC-24 финал | 10+10+10+20+15+15+20+30+15+30 |

Зависимости, которые надо помнить: HAC-33 меняет вопросы заглушки → HAC-40 перепроверяет числа демо после него · HAC-36 ждёт `last_fallback_reason` из HAC-19 · HAC-23 ждёт HAC-21, иначе переснимать · HAC-42 ждёт HAC-23 и HAC-40.

**Правило фазы 3:** `make gate` после каждого слияния. Трое правят работающий путь одновременно — это единственная защита от регресса.

**Фаза 2 закрыта в 14:55.** Критерий был один: зелёный `tests/test_e2e.py` в main. Он зелёный, влит, `make gate` запускает его одной командой.

**Контрольная проверка, проведена полностью:**

| Что | Результат |
| --- | --- |
| Гейт демо-пути `make gate` | 2 passed — все девять переходов раздела 11 кейса |
| Весь набор в main | 69 passed / 1 skipped |
| Сборка без кеша | exit 0 |
| Тесты в контейнере на свежем образе | 69 passed / 1 skipped |
| Живой прогон на поднятом приложении | черновик → 6 вопросов → карточка → редактор (предварительно 90) → публикация → 2-я позиция в каталоге → отклик `pending` → решение бизнеса `accepted`, `decided_at` проставлен |
| Семантика подтверждения | до публикации `score=0 status=draft confirmed=False`, после — `score=90 level=priority status=published confirmed=True` |
| Контракт `BRIEF.md` | сошёлся дословно во всех трёх лейнах, расхождений нет |

**Ловушка, на которую наступили и которая описана в README.** `data/store.json` переживает перезапуск, и сид повторно не читается: файл, созданный при раннем сиде, показывал 1 задачу вместо 5. Перед прогоном сценария — `make reset`. Файл в `.gitignore`, до эксперта не доедет.

**Открытое замечание по поверхности.** Интерфейс конструктора и редактора на английском (`Post a task`, `Clarify the task`, `Save changes`), при том что кейс, жюри и сами вопросы русские. Передано Анель комментарием в HAC-11, правка механическая. Файлы `business_new.html`, `business_questions.html`, `business_card_edit.html`.

**Главный риск — снят.** Ниже прежняя формулировка оставлена как след: она описывала состояние до 14:55.

**Главный риск (снято 14:55) — вход в сценарий.** Хвост демо-пути (каталог → отклик → решение бизнеса) закрыт и покрыт тестами. Не закрыто начало: HAC-10 (конструктор) по-прежнему лежит у Анель на локальной ветке `work` и **не влит в main** с 13:55, HAC-11 (редактор с публикацией) не начат. Следствия: у Анель **ноль коммитов в соревновательном окне** — это её личный риск незачёта участия, и закрыть его за неё нельзя, ветка только на её машине; без публикации карточку нельзя довести до каталога живьём, поэтому HAC-16 (сквозной смоук) пока делать не на чем.

**В работе:** HAC-10 → HAC-11 (Анель) · подача HAC-17 и README (капитан). Ядро (Дарья) свободно, следующий HAC-19.

**Следующий шаг:** Анель — влить HAC-10, затем HAC-11 (прочитать в нём «Уточнения от капитана» про подтверждение балла; редирект после публикации вести на `/tasks/{id}`, страница уже есть). Капитан — HAC-17 к **15:30**, затем HAC-16. Дарья — HAC-19 в своих файлах.

**Приёмка фазы 2 — один критерий, а не мнение.** Фаза 2 закрыта тогда и только тогда, когда `tests/test_e2e.py` зелёный в main. Файл написан и лежит на ветке `yessimkhanuly/hac-16-smoke`, сейчас намеренно красный: падает на `GET /business/new` → 404. Он проходит все девять переходов раздела 11 кейса за один проход — черновик, ≥3 вопроса, карточка без автопубликации, рост рейтинга до «готовой», подтверждение и публикация, позиция в каталоге, отклик, ручное решение бизнеса. Имена маршрутов и полей форм взяты из «Контракта» в `BRIEF.md` дословно, поэтому он же ловит разошедшийся контракт. Отдельного «слияния задач фазы 2» не существует: по процедуре слияние непрерывное, стык — это проверка и перепланирование.

**План Б, если HAC-11 не готов к 16:00:** редактор забирает капитан отдельным роутером по той же схеме, что каталог. Тогда Анель остаётся только конструктор, который у неё уже написан.

**Процессное отклонение 14:16:** `PROGRESS.md` правил не капитан (коммит `854272e`). Договорённость — файл ведёт только Claude капитана, иначе он превращается в конфликт. Содержание записи верное, оставлено как есть.

---

**Фаза 3 закрыта, кроме одного тикета (15:45).**

| Кто | Закрыто | Осталось |
| --- | --- | --- |
| Капитан | HAC-43 гигиена · HAC-37 тест сида · HAC-38 экранирование · HAC-39 позиция и счётчики · HAC-40 сверка демо · HAC-41 README ограничения · HAC-23 девять скриншотов · HAC-42 README демонстрация · **HAC-33 забран у Дарьи и закрыт** | HAC-22 прогон 2 в 18:00 · HAC-24 финал |
| Анель | HAC-30 · HAC-35 · HAC-20 · HAC-34 · HAC-21 · HAC-36 — **весь лейн поверхности закрыт** | — |
| Дарья | HAC-19 | **HAC-32 прогон с реальным ключом** — единственный незакрытый тикет фазы 3 |

**Скриншоты сняты** (`docs/screenshots/`, девять файлов) headless-браузером по коду main: состояние двигалось теми же запросами, что делает человек. README получил раздел «Демонстрация» с таймингом на 5 минут.

**HAC-33 забрал капитан** (тикет был Дарьин, она на HAC-32). Заглушка вопросов теперь цитирует черновик, даёт отраслевые примеры и помечает частично закрытые показатели — критерий «вопросы уместны», 15 баллов, и он важен именно потому, что эксперт скорее всего запустит проект **без ключа**. Числа демо не изменились: 30 «Черновик» → 100 «Приоритетная». Скриншот вопросов переснят. **Капитан трогал `app/ai.py` и `tests/test_ai.py` — Дарье перед продолжением HAC-32 обязателен `git pull --rebase`.**

**Единственный оставшийся риск — ключ API.** `tests/test_llm.py` до сих пор единственный skipped: путь с реальной моделью за день не запускался ни разу. Если эксперт вставит ключ и получит ошибку — это критерий «Техническая реализация» 25 баллов. HAC-32 у Дарьи.

---

**Проверка капитана 15:55–16:05 — перед ручным прогоном команды.**

| Что проверено | Результат |
| --- | --- |
| Тесты по файлам и целиком | все девять файлов зелёные, **135 passed, 0 skipped** |
| `make gate` | 2 passed, с ключом и без |
| Сборка без кеша + тесты в контейнере | exit 0 |
| **Фаззинг всех входов** — пустое, 6000 символов, эмодзи, HTML, SQL, обход пути, нулевой байт, мусор в отрасли, ссылке, решении, фильтрах, несуществующие id | **ни одного 5xx** |
| Отладочный мусор | нет `print`, `TODO`, `FIXME`, голых `except` |
| Пины | Dockerfile `python:3.12-slim`, все зависимости с `==` |

**Найдено и исправлено — три вещи:**

1. **Инцидент с ключом.** Ключ OpenAI был вписан в `.env.example` и `prep/templates/env.example` — оба файла **в git**. Поймано до коммита: история чистая, в отслеживаемых файлах ключа нет, проверено по `git rev-list --all`. Ключ перенесён в `.env` (в `.gitignore`), шаблоны возвращены к пустому значению. Отзывать ключ не нужно, он не публиковался.
2. **Контракт `BRIEF.md` расходился с кодом** в двух местах: не был записан маршрут `GET /business/drafts/{id}` (PRG из HAC-20) и расширение `/health` из HAC-35. Оба расхождения в нашу пользу, но контракт обязан описывать то, что есть.
3. **Баг устойчивости.** Ключ с не-ASCII символами давал `UnicodeEncodeError` из транспорта httpx; `llm.py` ловит только `OpenAIError`, и исключение пробивало наружу — веб отдал бы 500 вместо заглушки. Починено на границе `app/ai.py`: контракт «никогда не бросает» теперь соблюдается для любого типа исключения.

**HAC-32 закрыт, последний `skipped` исчез.** Путь с реальной моделью проверен: 6 содержательных вопросов, карточка собрана **только из сказанного** — семь полей остались пустыми, модель ничего не досочинила. Балл с ключом тот же, **30 «Черновик»**, то есть демо можно показывать и с ключом, и без.

**Фаза 3 закрыта полностью. Фаза 4 нарезана: 8 тикетов** — HAC-24 заморозка · HAC-45 вычитка README · HAC-46 секреты и авторство · HAC-47 финальная подача · HAC-48 финальный PROGRESS · HAC-49 чистый клон в 18:00 · HAC-50 Demo Day у Анель · HAC-51 доступность репозитория.

**Чеклист ручной приёмки:** `docs/acceptance.md`, девять блоков, ожидаемые значения сверены с кодом.

---

## Почасовая сетка

Заполняется по ходу соревновательной части. Время — Астана.

| Час | Время | Состояние на конец часа | Коммит |
| --- | --- | --- | --- |
| 0 | 13:00–13:27 | Кейс разобран, бриф и контракт в `BRIEF.md`, каркас FastAPI + модели + хранилище + заглушки ядра, 4 теста зелёных, тикеты в Linear | `e7d94f0` |
| 1 | 13:27–14:16 | Ядро закрыто целиком: обёртка LLM, рейтинг, уточняющие вопросы, сборка карточки с защитой от выдуманных фактов (HAC-6/7/8/9). Обвязка: Docker, сид, демо-сценарий с точными баллами, тесты хранилища, проверки воспроизводимости, инструкции для Codex. README: формула рейтинга и AI-функции. Поверхность не влита | `134aa64`, `d78cf76`, `467344b`, `f25ba8b`, `ef3f40d`, `1e39222` |
| 2 | 14:16–14:40 | Капитан забрал половину веб-слоя: каталог с сортировкой и фильтрами и страница задачи с откликами команд и ручным решением бизнеса, отдельным роутером без конфликта с конструктором (HAC-12, HAC-13). 62 теста зелёных | `287ccdf` |
| 3 | 14:40–14:55 | **Фаза 2 закрыта.** Приехали конструктор (HAC-10) и редактор с публикацией (HAC-11); написан и влит сквозной гейт `tests/test_e2e.py` + `make gate` (HAC-16). Контрольная проверка пройдена целиком: гейт, 69 тестов, сборка без кеша, тесты в контейнере, живой прогон всех девяти переходов | `eef81fa`, `676b0a1`, `4b0e0a2` |
| 4 | 14:40–14:52 | Стык фаз 2→3 по полному тексту ТЗ: `BRIEF.md` переписан дословно с рубрикой кейса, найдены три пункта не из конспекта (качество карточки, валидация, «тестовые сценарии»), тикеты фазы 3 пересобраны, новый HAC-30 | `e01bc02` |
| 5 | | | |

---

## Принятые решения

Что решили и почему. Нужно, чтобы не переобсуждать одно и то же и чтобы README потом собирался из готового.

| Решение | Причина | Когда |
| --- | --- | --- |
| Трек 07 «Образование» | Выбор команды, не окончательный | до старта |
| Docker как основной путь запуска + запасной путь без него | Критерий воспроизводимости, 20 баллов. Запасной путь — страховка от окружения эксперта | до старта |
| Ветку заводит исполнитель, как ему удобно; готовое сразу в main | Без ревью и PR — на это нет времени. Интеграция не откладывается на конец | до старта |
| Потоки работы и лейны нарезаются в фазе 0, а не заранее | Слои решения видно только по кейсу. Жёсткое закрепление людей за ролями привело бы к простою и к работе не над тем | до старта |
| Один провайдер в критическом пути — OpenAI | Два провайдера на основном сценарии = два способа упасть. NVIDIA по назначению: эмбеддинги, массовые вызовы, речь, резерв | до старта |
| Рейтинг: `score/level` карточки — только подтверждённый балл; редактор показывает предварительный | Кейс: «баллы только за заполненные и подтверждённые поля». Уточнение капитана по ревью брифа, 13:50 | 13:50 |
| NVIDIA-резерв не входит в приёмку | Наше решение, не требование кейса; при нехватке времени не проверяем | 13:50 |
| FastAPI + Jinja2, серверный HTML, без Node | Один процесс, один порт, одна команда запуска; три лейна делятся по файлам без конфликтов | 13:25 |
| JSON-хранилище вместо БД | Сид-данные кейса (5×4 сущностей) — это буквально начальный файл; SQL за 5 часов не окупается | 13:25 |
| Рейтинг детерминированный, без LLM | Критерий «прозрачен, пересчитывается» на 25 баллов — правила должны быть объяснимы в README и покрыты тестами | 13:25 |
| ИИ: две функции (вопросы, сборка карточки) + локальная заглушка без ключа | Кейс требует минимум одну и разрешает заглушку; эксперт без ключа всё равно проходит сценарий | 13:25 |
| `.env` необязателен для `docker compose up` | Воспроизводимость: без ключа — режим заглушки, а не падение | 13:35 |

---

## Известные проблемы и ограничения

На пятом часу этот список дословно уезжает в README. **Раздел «Ограничения» README уже написан
и сверен с кодом (HAC-41)** — сюда пишем только то, что появится после него.

| Что | Статус |
| --- | --- |
| Путь с реальным ключом OpenAI за день ни разу не запускался — `tests/test_llm.py` единственный skipped | HAC-32 у Дарьи, Urgent |
| Заглушка задаёт одинаковые вопросы для любого черновика — критерий «вопросы уместны» 15 баллов | HAC-33 у Дарьи |
| NVIDIA-резерв реализован, но в окне не проверен | принято, записано в README |
| `data/store.json` переживает перезапуск, сид повторно не читается | лечится `make reset`, описано в README |
| Скриншоты для Demo Day не сняты | HAC-23, после слияния всей поверхности |
