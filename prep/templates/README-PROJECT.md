# <Название проекта>

<!--
Структура повторяет список организатора из инструкции для участников —
именно по нему README и проверяют. Порядок и названия разделов не менять.

README заменяет предварительную презентацию команды. Его читают технические
эксперты и AI-судья, без живых пояснений с нашей стороны. 20 баллов.

Главное правило организатора: использовать только то, что подтверждается
репозиторием. Не описывать функции, технологии и результаты, которых в проекте нет.

Заполнять по ходу дня, а не на последнем часу. Подсказки в комментариях удалить.
-->

Трек 07 «Образование». Кейс: <название кейса>. Команда TayMas Systems, хакатон HackAlem AI.

## Краткое описание

<!-- Какую проблему решает проект и для кого. Два-три предложения. -->

## Что реализовано

<!-- Основные функции и возможности. Только фактически работающее:
критерий "Техническая реализация" прямо проверяет, что функции
не заменены заранее подготовленными ответами или имитацией. -->

- 
- 

## Как работает решение

<!-- Основной пользовательский сценарий от входных данных до результата.
Это то, что оценивается на 20 баллов по критерию соответствия кейсу. -->

## Технологии

<!-- Языки, фреймворки, библиотеки, AI-модели, API и внешние сервисы. -->

| Компонент | Что используется | Зачем |
| --- | --- | --- |
| | | |

Разработка велась с использованием AI-агента, как предписано инструкцией для участников и разрешено п. 6.9 Положения.

## Архитектура проекта

<!-- Основные компоненты и как они взаимодействуют. Текстом или схемой. -->

## Установка и запуск

### Системные требования

- 

### Переменные окружения

| Переменная | Обязательна | Назначение |
| --- | --- | --- |
| | | |

Пример заполнения — в `.env.example`. Реальные ключи в репозитории отсутствуют.

### Основной путь: Docker

```bash
cp .env.example .env
# заполнить .env
docker compose up --build
```

После запуска: <что открыть, куда обратиться>

### Запасной путь: без Docker

<!-- На случай, если у проверяющего нет Docker или он не запускается. -->

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# заполнить .env
python -m app
```

## Как проверить решение

<!-- Пример сценария, который может повторить жюри. Самый важный раздел
после "что реализовано". Конкретные шаги, конкретные входные данные,
ожидаемый результат — проверяющий не должен ни о чём догадываться. -->

1. 
2. 
3. 

Ожидаемый результат: <что именно должен увидеть проверяющий>

Автоматическая проверка:

```bash
make check
```

## Данные и интеграции

<!-- Какие источники данных, API и внешние сервисы используются,
откуда взяты данные и на каких условиях. -->

- 

## Ограничения

<!-- Что не реализовано и какие ограничения есть у текущей версии.
Осознанные границы выглядят сильнее, чем умолчание.
Берётся из раздела ограничений в PROGRESS.md. -->

- 

## Развёрнутая версия

<!-- Ссылка, если есть. Если нет — так и написать, это не обязательное требование:
развёрнутая версия является преимуществом, но не заменяет запуск из репозитория. -->

## Ранее созданные материалы

<!-- Обязательный раздел, п. 6.4 Положения: раскрыть использование ранее созданного
кода, сторонних библиотек, открытых моделей, шаблонов, датасетов и иных внешних
материалов. Нераскрытие — основание для дисквалификации по п. 12.2. -->

**Создано до начала соревновательной части:**

- Шаблоны инфраструктуры из `prep/templates/` — Dockerfile, docker-compose, Makefile, пример переменных окружения, каркас этого README. Доменной логики не содержат.
- Файлы `CLAUDE.md`, `HACKALEM-CONTEXT.md`, `PROGRESS.md` — рабочая документация команды.

**Сторонние библиотеки:** перечислены в `requirements.txt`.

**Модели и внешние сервисы:** перечислены в разделе «Данные и интеграции».

## Команда

TayMas Systems

- Azamat Yessimkhanuly — капитан
- Дарья
- Анель Алибек
