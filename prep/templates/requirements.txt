# Версии проверены 23.09.2026 на Python 3.14 и 3.12: ставятся без сборки из исходников.
# Пины нужны, чтобы эксперт собрал проект через неделю ровно тем же составом.
#
# Добавляя зависимость: сначала поставить без версии, потом `pip freeze` и вписать
# то, что реально встало. Пины по памяти не работают — такой версии может не существовать.

openai==3.19.0
pydantic==2.13.5
pydantic-settings==2.15.0
pytest==9.1.1
